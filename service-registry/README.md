# Service Registry

Eureka server used by all services for discovery.

**Port:** 8761

Access the dashboard at `http://localhost:8761`.

Run with:

```bash
cd service-registry
mvn spring-boot:run
```
